# Preregistration supplement 2: status rules `v0.2`, state-driven termination, prompt `reference-roles-prompt-v0.4`

Written on 2026-09-25, before any paid run under status rules `v0.2` or under
`reference-roles-prompt-v0.4`. It records owner ruling D1 (2026-09-24, item 6)
before the first calibration run.

It changes `v0.2-four-arm.md`'s table "What is fixed before any result exists"
in two ways:

- it supersedes the prompt-version row, which supplement 1 had already moved
  to v0.3 and which this supplement moves to v0.4;
- it adds a status-rules row, which that table did not have.

Every other row of `v0.2-four-arm.md` and supplement 1 stands as registered.

## What changed

| item | value |
| --- | --- |
| status rules | `v0.2` — see status-rules-v02.test.mjs › "bumps STATUS_RULES_VERSION to v0.2" |
| graph arm prompt version | `reference-roles-prompt-v0.4` — see conclusion-role.test.mjs › "REFERENCE_PROMPT_VERSION is reference-roles-prompt-v0.4" |
| termination | state-driven, in both the scripted-control arm and the graph-model arm — see lane-arms.test.mjs › "scriptedNodes(record) and modelNodes(record, port) both carry the canonical derive_hypothesis_state and termination_check nodes, proven by behaviour rather than identity or source text" |

## Why `supported` is unreachable on this code

`supported` requires at least one confirmed prediction. No path in this code
produces a prediction, so no hypothesis can be confirmed. See
status-rules-v02.test.mjs › "never promotes a corroborated hypothesis to
supported without a confirmed prediction of its own".

## What `corroborated` means

`corroborated` means consistent with the available evidence, independently. It
does not mean the hypothesis survived a prediction test.

- It takes at least two independent medium- or high-strength supports, no
  material contradiction, and no confirmed prediction.
- `supported` keeps its v0.1 meaning: the same requirements, plus at least one
  confirmed prediction.
- See status-rules-v02.test.mjs › "pins the confirmed-prediction boundary
  between supported and corroborated".

The conclusion role is shown a definition of every status. Each definition is
generated from the rule table itself, so it cannot drift from the table. The
corroborated definition quotes that rule's own copy of the support
requirements. A test keeps this copy equal to the one the derivation applies.
See:

- conclusion-role.test.mjs › "a mutated table (minimumIndependentSupports 3)
  yields a different corroborated sentence containing 3, proving the text is
  generated";
- conclusion-role.test.mjs › "propose_conclusion's system prompt contains
  every sentence describeStatusRules(STATUS_RULES[STATUS_RULES_VERSION])
  returns";
- conclusion-role.test.mjs › "in every STATUS_RULES table, corroborated's
  support requirements equal supported's, which are the ones
  deriveHypothesisStatus applies to both".

## Termination uses corroboration, not prediction-confirmed support

Termination is decided from state by rules T0 to T6. They are applied in
order, and the first rule that matches decides. Here `r` is the number of
challenge rounds already run.

- **T0.** No evidence, at least one trial, and no trial ok: `tools-unavailable`. See
  state-termination.test.mjs › "T0: empty evidence with a non-ok trial history
  is tools-unavailable, guarding the architecture rule that a tool outage is
  reported honestly rather than read as \"no hypotheses found\"".
- **T1.** No hypotheses: `stalled`. See state-termination.test.mjs › "T1: no
  hypotheses at all is stalled, even with evidence and a successful trial on
  record".
- **T2.** `r` is 0: the leader is challenged. See state-termination.test.mjs ›
  "T2: challengeRounds === 0 always names the leader for a mandatory first
  challenge, before any competing-leader or leadership-change rule is even
  consulted".
- **T3.** Two or more hypotheses at corroborated or above: challenge again.
  See state-termination.test.mjs › "T3: two hypotheses at corroborated-or-above
  after the first round is challenge-required, naming the earlier one in state
  order as leader on the tie".
- **T4.** `r` is below the round cap, and the leader is the newest challenge
  alternative: one more round. See state-termination.test.mjs › "T4:
  leadership just passed to the newest challenge alternative - one more
  mandatory round before it can be called sufficient (policy 2)".
  - Its limit: a change of leadership is detected only as "the leader is the
    newest challenge alternative". Leadership returning to an earlier
    hypothesis is read as no change. See state-termination.test.mjs › "T4
    limit: an earlier hypothesis that leads while a newer challenge
    alternative exists is read as no leadership change, so it is sufficient
    without another round".
- **T5.** A sole hypothesis at corroborated or above: `sufficient`. See
  state-termination.test.mjs › "T5: a sole corroborated leader that was never
  challenged is sufficient once at least one round has run".
- **T6.** Otherwise: `stalled`. See state-termination.test.mjs › "T6: after a
  round has run, no hypothesis reaching corroborated or above is stalled, not
  sufficient by default".

Outside T0 to T6, the graph kernel bounds the challenge loop: at the round
cap, a further challenge becomes `ambiguous`. See state-termination.test.mjs ›
"through the real kernel: two corroborated hypotheses at the challenge round
cap terminates ambiguous, not challenge-required forever".

Termination reads state, never the scenario id or ground truth. See
lane-arms.test.mjs › "scriptedNodes(record)'s termination depends on state,
never on the scenario id or ground truth: a renamed clone of a real
calibration scenario reaches the same stop kind as the original, and a
sparse-evidence variant reaches a different one".

## The prediction path

This code implements no prediction path.

Owner ruling D1 put the prediction path after calibration measurements (items
3, 6 and 8). A later owner direction changed that order for a deterministic
path. It is recorded on 2026-09-24 in project ticket AIC-122, "Complete
executable investigation loop before four-arm calibration", under "Owner
direction" and "Calibration gate". The ticket says:

- the prediction path is deterministic and is built before the paid four-arm
  calibration (AIC-121);
- it adds no fifth model role and no LLM judge.

AIC-122 is split into AIC-123 to AIC-126, which run in that order. On the
board, AIC-122 and AIC-126 each block AIC-121. This supplement records that
order; it does not decide it.

Item 8 of the ruling still stands for everything beyond that deterministic
path: the decision rests on the measured prediction gap below. The
deterministic path will be registered in a further supplement before AIC-121
runs.

## The prediction-gap diagnostic

Calibration reports, for the scripted-control and graph-model arms, how many
runs stopped short of a stronger state because no confirmed prediction exists
(ruling item 7). See prediction-gap.test.mjs › "the live lane reports
predictionGapCounts for the control and model arms, equal to a hand-computed
aggregate of their own results, and reports the measured calibration control
fact that every run stalls with no corroborated hypothesis". The diagnostic:

- **Is not a metric.**
  - It is not compared between arms.
  - It is not part of the control baseline.
  - It is never a feedback row.
  - It is never an AIC-19 quality measure.
  - See prediction-gap.test.mjs › "none of the prediction-gap keys appear in
    BENCHMARK_METRIC_KEYS or BEHAVIOR_METRIC_KEYS", › "none of the
    prediction-gap keys appear in a graph-executed result's metrics or
    behaviorMetrics, in observedBaseline, or in graphVsNaive" and ›
    "persisting a result carrying predictionGap emits no feedback row keyed by
    any prediction-gap field".
- **Counts each flag separately. The five counts are not a partition of the
  runs.** See prediction-gap.test.mjs › "two corroborated hypotheses that
  terminated ambiguous report stalledLeaderLacksConfirmedPrediction on the
  earlier-listed leader".
- **Reports the prediction gap as two counts, read separately and never
  summed.** The option-P decision (ruling item 8) reads both:
  - `stalledLeaderLacksConfirmedPrediction` counts runs that stopped short with
    a corroborated leader that lacked a confirmed prediction;
  - `sufficientFromCorroborated` counts runs that concluded on corroboration
    alone, never on a confirmed prediction.

## Known confound: single decisive evidence

Checked against the fixtures on 2026-09-25:

- In `dependency-caused-incident-b` and `challenge-changes-leader`, only one ok
  evidence item bears on the true cause without being declared misleading. The
  true cause can therefore reach `corroborated` only if the model counts the
  misleading item as support. See prereg-confounds.test.mjs ›
  "dependency-caused-incident-b carries exactly one distinct, non-misleading,
  expected ok evidence id (single-decisive-evidence confound)" and ›
  "challenge-changes-leader carries exactly one distinct, non-misleading,
  expected ok evidence id (single-decisive-evidence confound)".
- `multiple-plausible-causes` expects `ambiguous`. It carries two ok evidence
  ids in total, so two hypotheses can both be corroborated only if both items
  support both hypotheses. See prereg-confounds.test.mjs ›
  "multiple-plausible-causes carries exactly two distinct ok evidence ids in
  total, in contrast to the single-decisive-evidence scenarios above".

## No fitting

Scenarios and ground truth are not fitted to the new rule. Their bytes are
frozen, and a change to them is a new, dated, preregistered decision, never an
edit. See replay-scenarios.test.mjs › "freezes every v0.2 benchmark scenario
byte for byte, with no id missing and none extra: a change is a new, dated,
preregistered decision, never an edit" and › "freezes STRUCTURAL_GROUND_TRUTH
byte for byte: a change is a new, dated, preregistered decision, never an edit
to fit a status-rules result".
