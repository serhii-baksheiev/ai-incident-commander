# Preregistration supplement 1 — prompt version reference-roles-prompt-v0.3

Written 2026-09-24, before any paid run under `reference-roles-prompt-v0.3`.
It supersedes ONLY the prompt-version row of `v0.2-four-arm.md`'s "What is
fixed before any result exists" table. Every other row of that file — the
arms, the model, sampling, the evaluator, information mode, incident identity,
the corpus partition, runs per scenario, candidate identity, the metrics and
where each applies, the expected directions, the reading rules and the known
confounds — is unchanged and stays exactly as registered there.

## What changed under this prompt version

- `propose_conclusion` is now wired into the graph arm's model-backed nodes,
  alongside `generate_hypotheses`, `interpret_residual_evidence` and
  `challenge_hypothesis` — see lane-arms.test.mjs › "modelNodes(record, port).propose_conclusion is a model role: the fake port sees exactly one call, carrying the mechanism vocabulary sentence built from evals.ROOT_CAUSE_MECHANISMS"
- `interpret_residual_evidence`'s system prompt now states the id contract:
  an assessment must name an evidenceId, hypothesisId and, if present, a
  predictionId shown in the state below, and any other id is refused — see
  roles-model-nodes.test.mjs › "the system prompt states the id contract: an assessment must name an evidenceId, hypothesisId and (optional) predictionId shown in the state below, and any other id is refused"

`v0.2-four-arm.md` already registered, in advance, that AIC-119 would add a
conclusion role and record its prompt version in run metadata — this
supplement is that registration keeping pace with the prompt version the role
shipped under, not a later deviation.
