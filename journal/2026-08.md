# 2026-08

### Isolated self-hosted runner shipped; Jira loop stopped on unchanged reversed links

- **done** — merged self-hosted runner [PR #6](https://github.com/serhii-baksheiev/ai-incident-commander/pull/6) as `d0ad96074a416261c9d8238a7651d8b582b13a75`: CI now runs inside the isolated Lima ARM64 VM as dedicated non-sudo user `aic-runner`; [exact-head run `33008181354`](https://github.com/serhii-baksheiev/ai-incident-commander/actions/runs/33008181354) and [post-merge run `33008628082`](https://github.com/serhii-baksheiev/ai-incident-commander/actions/runs/33008628082) both succeeded, and `linux-arm64-01` returned online and idle; moved [AIC-52](https://sbaksheiev.atlassian.net/browse/AIC-52) to `Done`, cleared its queue labels, and recorded completion in comment `14668`
- **reviewed** — final runner head `b09bca6772638f3a49112cd92635a3b21806a8f7` passed 16/16 tests, Lima validation, live least-privilege and mount preflight, exact-head reviewer coverage, and code, prose, and security gates with no blockers before merge
- **stopped at** — `queue-data-anomaly` after a fresh connector snapshot of 50 eligible Jira issues on merge head `d0ad96074a416261c9d8238a7651d8b582b13a75`: the repository's own `jira.toTicket` → `core.selectNext` path still selects milestone AIC-14 because the native `Blocks` links remain opposite to the dependency prose; no implementation ticket was claimed
- **unblocked** — nothing was unblocked; AIC-52 was the runner operator item, while no delivery dependency pointed to it
- **queue hygiene** — [AIC-51](https://sbaksheiev.atlassian.net/browse/AIC-51) comment `14669` carries the current exact-head reproduction: link `11610` maps AIC-2 as blocked by AIC-3 and link `11623` maps AIC-13 as blocked by AIC-14; the available Atlassian connector still exposes link creation but no link deletion, and issue edit rejects `issuelinks`, so the run did not add opposite links and create cycles; safe continuation requires deleting and recreating those native links in the direction stated by the tickets
- **cost** — for this checkpoint before its own review: test-writer subagents: 1; runner CI runs observed: 2; deploys: 0; the exact earlier reviewer count was not retained across compaction and is not estimated
- **run evidence** — checkout-local connector trace `.claude/runs/20260827-000812-connector-loop` in the main checkout, ended with `queue-data-anomaly`; runner PR and CI links above; the older unsafe macOS branch/worktree `chore/self-hosted-ci` remains preserved as security-HOLD evidence and was not merged

### Self-hosted runner rolled back; Jira loop still needs link deletion

- **done** — implemented dedicated macOS ARM64 CI on remote branch `chore/self-hosted-ci` after its targeted contract test first failed 0/2, verified a live `mac-arm64-01` registration and LaunchAgent, then rolled the live runner back after the final security gate: stopped and uninstalled the service, deregistered it, removed its credential files, confirmed the repository has zero registered runners, and moved the dedicated directory to the recoverable Trash path `/Users/eru/.Trash/actions-runner-ai-incident-commander-20260826-security-hold`; filed operator item [AIC-52](https://sbaksheiev.atlassian.net/browse/AIC-52)
- **escalated** — `pr-ship` round 2/2 held head `4e4889a411aa5a16f61c8703f09c9b0ebebdd812`: pull-request tests would run as the maintainer's macOS UID, which can read unrelated GitHub/SSH credentials and other repository runner state; no PR was opened and the branch was not merged; remediation requires a dedicated unprivileged OS user or isolated VM/container, while non-interactive `sudo` and a running local container/VM backend were unavailable
- **reviewed** — round 1 returned code/security `SHIP` and prose `HOLD` on fail-fast checksum enforcement; its targeted test then failed 1/2 before the fix was added; round 2 returned code/prose `SHIP` and security `HOLD` on host isolation, with complete exact-head fan-out coverage
- **stopped at** — `security-gate-hold` for self-hosted CI and `queue-data-anomaly` for Jira; the owner delegated merge and delivery decisions, but neither permission overrides a blocking security finding or permits adding inverse Jira links on top of undeletable existing links
- **unblocked** — nothing was unblocked and no Jira item was closed
- **queue hygiene** — the latest comment on [AIC-51](https://sbaksheiev.atlassian.net/browse/AIC-51) supersedes its two earlier contradictory comments: live `Blocks` links are reversed relative to `Depends on` prose, the Rig adapter is not the defect, and AR-150's premise is false; the connector exposes link creation but not deletion, issue edit rejects `issuelinks`, and no connected browser was available, so the run did not create opposite links that would form cycles; AIC-51 and AIC-52 remain `To Do` with `operator-queue` and `triage`
- **cost** — before review of this stop record: reviewer/gate subagents: 8; test-writer subagents: 1; CI runs: 0; deploys: 0
- **run evidence** — self-hosted gate trace `.claude/runs/20260826-self-hosted-ci`; remote held branch `chore/self-hosted-ci`; Jira connector issue comments `14656`, `14657`, and superseding `14662`

### Connector-backed loop stopped on reversed Jira dependencies

- **escalated** — outcome `documented-stall` at queue selection: the authorized Atlassian connector returned 50 open AIC items, and the repository's own `jira.toTicket` → `core.selectNext` path selected `AIC-14`; observed that `AIC-14` says it is not selectable until `AIC-13` is Done while its native link says `AIC-14 blocks AIC-13`, and that `AIC-2` says `Depends on: None` while Jira says it is blocked by `AIC-3`; no implementation or claim was attempted; current hypothesis is that the roadmap's Blocks endpoints were created inward/outward in reverse; filed operator escalation [AIC-51](https://sbaksheiev.atlassian.net/browse/AIC-51); the single unblocking question is whether the Blocks links should be reversed to match each item's `Depends on` and entry-gate prose, making `AIC-2` first and `AIC-13` block `AIC-14`
- **reviewed** — no implementation review ran because queue integrity stopped the run before a task was claimed; `AIC-14` remained `To Do`
- **stopped at** — `queue-data-anomaly`; native links are authoritative, so the run did not silently take `AIC-2` or repair Jira metadata
- **unblocked** — nothing was unblocked; no Jira item was closed
- **queue hygiene** — [AIC-51](https://sbaksheiev.atlassian.net/browse/AIC-51) is `To Do` with `operator-queue` and `triage`; connector OAuth has `read:jira-work` and `write:jira-work`, while the shell `JIRA_*` credentials remain unset and the tracked config remains `plan-md`
- **cost** — before publishing this stop record: reviewers: 0; CI runs: 0; deploys: 0
- **run evidence** — `.claude/runs/20260826-211601-connector`; attended budget limited this connector-backed run to at most one normal Jira item
- **preflight** —

  ```text
  **preflight** — verdict: CAUTION

  - pass · killSwitch — absent
  - pass · runDirNotExported — not exported
  - pass · defaultBranchFresh — main == origin/main
  - unknown · lastDeploy — could not read deploy history: Command failed: gh run list --workflow deploy --limit 1 --json conclusion,url

  _Not checked by this script — still yours (3):_
  - the queue is reachable through its adapter (`node .claude/scripts/queue/index.mjs next`)
  - no stray worktree from a dead session that this run might mistake for its own
  - a budget is declared for this run, and it is written down somewhere the run can re-read

  _An item skipped twice is the signal to script it or drop it: a checklist
  nobody completes decays into one nobody reads._
  ```

  Manual completion: Atlassian connector access was live and normalized through `core.mjs`; no stray worktree existed; the budget was written before selection.

### Declared loop stopped on the empty configured queue

- **stopped at** — `queue-empty`; the configured `plan-md` adapter returned no ticket, no skipped items, and a successful stop, so the run did not substitute Jira or invent work
- **unblocked** — this queue has no dependency links; no ticket was selected or closed
- **queue hygiene** — 0 items and 0 proposals checked; nothing stale was reported
- **cost** — before publishing this stop record: reviewers: 0; CI runs: 0; deploys: 0
- **run evidence** — `.claude/runs/20260826-204155-codex`; budget declared as at most 4 reviewer subagents, 3 CI runs, and 1 deploy, with no task-count ceiling
- **preflight** —

  ```text
  **preflight** — verdict: CAUTION

  - pass · killSwitch — absent
  - pass · runDirNotExported — not exported
  - pass · defaultBranchFresh — main == origin/main
  - unknown · lastDeploy — could not read deploy history: Command failed: gh run list --workflow deploy --limit 1 --json conclusion,url

  _Not checked by this script — still yours (3):_
  - the queue is reachable through its adapter (`node .claude/scripts/queue/index.mjs next`)
  - no stray worktree from a dead session that this run might mistake for its own
  - a budget is declared for this run, and it is written down somewhere the run can re-read

  _An item skipped twice is the signal to script it or drop it: a checklist
  nobody completes decays into one nobody reads._
  ```

  Manual completion: configured `plan-md` returned `queue-empty`; no stray worktree existed; the budget was written before selection.

### Jira connector reachable; adapter still missing shell credentials

- **done** — merged README/docs PR [#1](https://github.com/serhii-baksheiev/ai-incident-commander/pull/1) as `558eba3`; confirmed the live Atlassian connector still has Jira read/write scopes; re-read AIC-2 as `To Do` with no assignee
- **escalated** — outcome `documented-stall` at queue selection: the declared run invoked the repository Jira adapter with project `AIC` and observed `the jira adapter needs JIRA_BASE_URL, JIRA_EMAIL, JIRA_API_TOKEN in the environment. Set them in your shell or your secret manager — never in a file in this repo.`; an interactive zsh also reported all three variables unset, and the only Jira reference in `~/.zshrc` is a comment; current hypothesis is that Atlassian connector OAuth is healthy but no Jira REST credentials are inherited by the Codex process; the one unblocking question is whether the owner will export all three variables in the parent environment of a fresh Codex session and confirm the intended AIC-2/AIC-3 native-link direction
- **reviewed** — no implementation review ran because selection never produced a task; AIC-2 remained `To Do` and unclaimed
- **stopped at** — `queue-unreadable`; the loop did not substitute connector results or the empty local PLAN queue for adapter selection
- **unblocked** — unobserved: no ticket was closed because selection never completed
- **queue hygiene** — `.claude/queue.json` still names `plan-md` even though the owner selected Jira project AIC; the run used an explicit temporary Jira adapter config for the reachability attempt and did not silently rewrite the tracked queue configuration; AIC-2 says it has no dependency and AIC-3 says it depends on AIC-2, but the native Jira link currently makes AIC-2 blocked by AIC-3, so the adapter would treat the link as authoritative and hold AIC-2 until a human resolves the conflict
- **cost** — reviewers: 0; CI runs: 0; deploys: 0
- **preflight** —

  ```text
  **preflight** — verdict: CAUTION

  - pass · killSwitch — absent
  - pass · runDirNotExported — not exported
  - pass · defaultBranchFresh — main == origin/main
  - unknown · lastDeploy — no deploy workflow exists

  Not checked by the script and completed manually:
  - queue reachability — failed through the Jira adapter with missing environment credentials
  - stray worktrees — none before this journal-only worktree was created
  - budget — first attended run, at most one normal Jira item
  ```

### Jira access confirmed; local AIC loop stopped before claim

- **done** — confirmed connected Atlassian access to project AIC with Jira read/write scopes; read AIC-2 and verified it is the first unblocked implementation ticket while AIC-3 depends on it
- **escalated** — outcome `documented-stall` at queue selection: the local Jira adapter failed with `Error: the jira adapter needs JIRA_BASE_URL, JIRA_EMAIL, JIRA_API_TOKEN in the environment. Set them in your shell or your secret manager — never in a file in this repo.`; tried the repository adapter directly against project AIC after confirming connector access; current hypothesis is that the Codex process inherited the Atlassian connector session but not the three environment credentials required by the Node adapter; the one unblocking question is whether the owner can export those three variables into a fresh Codex session (without committing them) and allow `.claude/queue.json` to be switched from `plan-md` to Jira project `AIC`
- **stopped at** — `queue-unreadable`; no Jira issue was claimed and no implementation files were changed
- **unblocked** — unobserved: no ticket was closed because selection never completed
- **queue hygiene** — `.claude/queue.json` still names `plan-md`, whose Agent queue is empty, while the intended live queue is Jira project AIC; this was reported and not silently corrected
- **cost** — reviewers: 0; CI runs: 0; deploys: 0
