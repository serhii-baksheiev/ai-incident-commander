# September 2026

### AIC-67 hardened four metric paths; five gate rounds, and the enumeration was deleted rather than repaired a fifth time

- **done** — AIC-67 merged as `b3b0735` ([#42](https://github.com/serhii-baksheiev/ai-incident-commander/pull/42)), Done in Jira, `transitioned: true` read back. Own-data-property reads and CreateDataProperty writes across `requireMetrics`, `requireBehaviorMetrics`, `projectRunMetadata` and the upstream `behaviorMetrics` construction in `packages/evals`. 376/376 at `76bab7d` to **409/409** at the merge, +32 tests and +1 for the fixture file `node --test` counts as a file declaring none. The queue drift the owner authorised was fixed for **one** ticket only: `triage` off AIC-67, `agent-queue` on, promotion recorded as an owner act because `core.mjs` calls it one
- **reviewed** — five `pr-ship` rounds, `code-reviewer` and `security-scanner` on every one, coverage complete each time. R1 **HOLD** on two blockers: `record.metadata.evaluatorVersion` still a plain `[[Get]]` gating `requireBehaviorMetrics`, which published versioned behaviour metrics while the metadata declared none — the item's own finding (2), and a divergence **this branch created**; and the `score` type check was a fix with no test, deleting it left the suite fully green. R2 and R3 **HOLD** on one comment paragraph. R4 and R5 **SHIP**, both reviewers, `security-scanner` reversing its two prior rulings and closing with "Merge it"
- **the finding worth keeping** — four hand-written inventories of the residual prototype-chain reads produced four different subsets, each fixing the last omission and introducing the next. My own mechanical pass missed the same two classes, because it grepped dotted reads on named identifiers and saw neither computed index access nor parameter-position destructuring. The two found last are the most serious in the ticket and both pre-exist on `main`: an inherited `client` on the options object redirects **every** outbound call in the layer to an attacker's client (a destructuring default fires only on `undefined`), and a hole in a length-parity-passing `results` array publishes a fabricated evaluation **under a real record's identity**. `.claude/rules/invariants.md` predicts exactly this and names the remedy — rewording is not an exit; delete, or make it a pointer. The enumeration was deleted; the header now states the boundary instead
- **mutation proof** — ten mutations on committed trees in a scratch clone, each reverted, none returning zero; every one of the 32 tests is covered. Two were the gate's own rather than the author's: neutering `ownValue` turns 23 red, and deleting the `score` check turns exactly one red where before the R1 fix it turned none
- **stopped at** — checkpoint, still running. AIC-67 closed; the next item in the owner's sequence is AIC-65, still `triage` and not yet promoted
- **unblocked** — **nothing was waiting.** The Jira board carries dependency links and AIC-67 had none in either direction, so this close released no item. AIC-18 remains blocked only by its own labels, not by links: `blocked-aic-62` and `blocked-aic-63` are stale, both blockers being Done
- **queue hygiene** — reported, not corrected: AIC-18 still carries `blocked-aic-62` and `blocked-aic-63` while both are Done, and `hygiene` reports AIC-64, AIC-65 and AIC-66 as `proposal-possibly-overtaken` against `f8342a1`, with AIC-51 `proposal-asof-missing`. `queue.state.json` now records `elevated-mechanism`, because every task's `.rig/claims/<id>.json` sits under a declared elevated path — so **every** close through this adapter records the mechanism tier and spaces the next elevated item, whether or not the work touched a mechanism
- **gate rounds** — the cap is 2 and the counter refused at rounds 3, 4 and 5 with `exit 2`. Each refusal stands unaltered in the record and `options.maxGateRounds` was **not** raised; the owner authorised each round explicitly, which is the AIC-63 precedent. The counter records 5
- **my own errors, recorded because the run is the only witness** — (1) a diagnostic call while debugging a `proposeTriage` failure **created AIC-69 with placeholder text**; I corrected that issue in place rather than filing a second. The underlying failure: `proposeTriage` builds the Jira summary from `change`, and mine was 2186 characters. (2) I wrote a pointer to a then-nonexistent AIC-69 and removed it, then re-added it once the ticket existed. (3) Three of the five gate rounds were spent on prose I wrote and had to be told four times was still a subset
- **filed** — AIC-69 (`triage`, not selectable) carries all eight measured surfaces plus the by-reference return and the `NaN`/`Infinity` score gap, so the evidence outlives the enumeration removed from the tree. Its acceptance criterion is **the test, not a better list** — a check that goes red when a non-`ownValue` read on caller-supplied data is added to the file. Two cosmetic slips in it are corrected by comment: a missing `createProject` and line refs gone +7 stale
- **cost** — reviewer subagents 12 (2 `check-premises`, 2 `test-writer`, 4 `code-reviewer`, 4 `security-scanner`); CI runs 5, one per pushed head, no re-runs; deploys 0

### Queue moved from the empty PLAN.md list to the Jira board, on an owner decision

- **owner decision** — the owner was shown that `.claude/queue.json` named `plan-md` while every v0.2 ticket lives in Jira, that the Agent queue in `PLAN.md` is empty so `queue/index.mjs list` returned `candidates: 0`, and that `plan-md` cannot produce a claim at all; presented with switching the adapter, adding a PLAN.md line, or working with no claim, the owner chose to switch to the Jira adapter. Recorded here because prior runs escalated exactly this question and deliberately left the config alone (`journal/2026-08.md` and the AIC-13 entry below), so the change must not read as a run quietly re-aiming its own queue
- **done** — `.claude/queue.json` now names `jira` with a JQL narrowed to `labels = "agent-queue"`, so 2 tickets are ingested rather than all 44 non-Done AIC issues; `PLAN.md`, `.claude/rules/workflow.md`, `CLAUDE.md`, `AGENTS.md` and `README.md` updated so none of them still claims PLAN.md is the queue or that a PLAN.md edit closes a task
- **why the narrowing** — the wide default rested entirely on link hygiene, and link hygiene was measurably broken the same hour: three chain links were recorded backwards (`AIC-63 blocks AIC-62` instead of the reverse), which made the selector offer AIC-18, a ticket that must stay blocked. Links fixed in Jira; evidence in AIC-62 comment `15677`
- **evidence** — `npm run check` 272/272 PASS; `decision-router` lane `model`, risk flag `elevated-path`; with the new config `candidates: 1, SELECTED: AIC-62` and AIC-63 held behind it; `revalidate --point BEFORE_PR --ticket AIC-62` was `UNVERIFIABLE` before the switch because `.rig/claims/` has never existed in this repository
- **reviewers** — first round on `e47d3a9`: security-scanner SHIP; code-reviewer HOLD (unrecorded Tier-2 decision, and an overstated `workflowClaim` claim); prose-reviewer HOLD (PLAN.md contradicted its own framing, the same overstatement, and a now-false line in `workflow.md`). All blockers fixed rather than argued with; a second round runs on the new head
- **unblocked** — nothing was released. No item was closed here and this branch is not merged; the Jira board does carry dependency links, and after the link fix AIC-62 reads `blockedBy=[]`, but it stays To Do because a claim cannot be recorded until this change lands — which is the whole reason it exists
- **not done** — no `human-review` label was applied by the agent, and no AIC-62 implementation is in this change. AIC-19 still carries the same reversed-link problem against the parked AIC-53..AIC-60 and is left alone: it is not selectable, and fixing it is not this change's scope
- **rig defect** — RP-99 (`reconcile-external-prs.mjs` asks gh for `authorAssociation`, which gh 2.67.0 does not have, and misreports it as gh being absent or unauthenticated) was not hit here: no lane reconciliation was needed. Not fixed from this repository

### AIC-13 LangSmith acceptance evidence held on missing credentials

- **done** — fetched `origin/main` and confirmed the prior acceptance head is unchanged at `ac569a04cc1903c49d847e11b0c3b98ab4b9d597`; read AIC-13 fresh through Rovo; recorded the credential audit and owner action in Jira comment `15429`; changed no product, benchmark, Rig or Agent-OS files
- **escalated** — the LangSmith prerequisite stage cannot supply an authenticated client: the current process has neither `LANGSMITH_API_KEY` nor compatible `LANGCHAIN_API_KEY`, no alternate endpoint/workspace configuration or supported config file was found, and the installed SDK reports `apiKeyPresent: false`; no network experiment was attempted and rows 21, 22 and 25 remain `UNPROVEN`; the single unblock is for the owner to restart Codex from a shell exporting a valid LangSmith key, plus endpoint/workspace variables only when the account requires them
- **stopped at** — `documented-stall`: LangSmith acceptance prerequisite unavailable, so AIC-13 remains open and the loop stops before mutation or v0.2 selection
- **unblocked** — nothing; AIC-13 did not close and its Jira link continues to block AIC-14
- **queue hygiene** — the installed `plan-md` adapter does not carry this owner-directed Jira continuation; live issue state was read through Rovo and the queue configuration was not changed
- **cost** — acceptance stage before this journal-only PR gate: reviewer subagents 0; CI runs 0; deploys 0
