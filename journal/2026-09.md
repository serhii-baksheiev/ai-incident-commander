# 2026-09

### AIC-13 acceptance held on missing external LangSmith evidence

- **done** — independently verified the frozen v0.1 DoD on exact current `main` revision `ac569a04cc1903c49d847e11b0c3b98ab4b9d597`: clean install and audit, dependency/architecture guards, build, 178/178 full tests, 95/95 focused benchmark/persistence/challenge/HITL tests, exact-main Linux ARM64 CI run `33476000617`, and a run-local StateGraph acceptance script exercising all four conclusion kinds. Jira comment `15427` carries the complete 41-row matrix: 38 PASS, 0 FAIL, 3 UNPROVEN. No product, Rig, generated Agent-OS, or v0.2 file changed.
- **escalated** — mandatory rows for an accepted v0.1 LangSmith baseline, a comparable changed experiment, and accepted-result read-back after restoration are UNPROVEN. AIC-10 comment `14941` explicitly records that no credentialed live LangSmith request was made; repository and Jira remote-link reads contain no native dataset/experiment ID or URL. Local fake-client and mutation tests prove the persistence/gate seams but cannot prove external experiment existence. The single unblock is evidence-only: run and read back a real LangSmith dataset, accepted baseline, changed experiment, evaluator feedback and post-restoration result tied to the tested SHA; no new product behavior is authorized.
- **reviewed** — `check-premises` returned `PREMISES_HOLD` with no blocker to running the gate and an advisory not to treat the LangSmith seam or Done tickets as acceptance. Fresh current-head verification then passed `npm run check` (178/178), the five focused acceptance suites (95/95), exact conclusion-kind execution, `npm audit --audit-level=high`, and exact-main CI/read-back. Historical AIC-2 through AIC-12 comments were read only for lineage and revalidated against current code/tests.
- **stopped at** — `documented-stall`: AIC-13 remains In Progress with resolution unset and label `escalated`; closure criteria prohibit Done while any mandatory row is UNPROVEN. AIC-1 was not closed, AIC-14/v0.2 was not selected, and no external LangSmith claim was invented.
- **unblocked** — nothing was released; AIC-13 still blocks formal v0.1 acceptance and the v0.2 Jira dependants remain untouched.
- **queue hygiene** — the work was explicitly owner-directed from fresh Jira AIC-13, while the currently installed `plan-md` queue remains empty and was not manually re-aimed. AIC-2 through AIC-12 being Done was treated as dependency state only, never proof.
- **cost** — reviewer subagent runs: 1 (`check-premises`); CI runs triggered/rerun: 0; exact-main CI runs read back: 1; deploys: 0.
- **run evidence** — `.claude/runs/20260901-101441-codex-aic13-acceptance`; Jira AIC-13 comments `15426` and `15427`; exact SHA `ac569a04cc1903c49d847e11b0c3b98ab4b9d597`; GitHub Actions run `33476000617`, job `99755275343`.
- **preflight** — `CAUTION`: kill switch absent; `RIG_RUN_DIR` initially unexported; `main == origin/main`; deploy history unavailable. A fresh run directory and explicit gate-only budget were declared before verification; no create-agent-rig upgrade or generated 0.6.2 workaround was attempted.

### AIC-12 merged cleanly and the installed loop stopped empty

- **done** — after the owner completed human review and explicitly authorized merge, PR #28 was squash-merged from reviewed head `15ec53f3dfb1a47880919f3287b9c92b4d5cc0f2` as `ac569a04cc1903c49d847e11b0c3b98ab4b9d597`; required pre-merge checks and exact-head code/security/`pr-ship` gates were SHIP, and the automatic post-merge `main` CI run `33476000617` passed.
- **reviewed** — the merge reused the already completed gate round 2/2 for the unchanged exact head: `code-reviewer` SHIP, `security-scanner` SHIP, `pr-ship` SHIP, local dependency boundaries/lint/build and 178/178 tests green. The owner supplied the required human review before authorizing merge.
- **stopped at** — after Jira write-back and task cleanup, the fresh installed `plan-md` selector returned `queue-empty`; no Jira item was substituted into the empty local queue.
- **unblocked** — AIC-13: native Jira link read-back now shows its AIC-12 blocker in Done. No dependent labels were edited manually.
- **queue hygiene** — 0 items and 0 proposals checked; nothing stale was reported. AIC-13 remains `To Do` in Jira but is not present in the installed `plan-md` queue.
- **cost** — before review of this stop record: reviewer subagent runs: 0; CI runs: 1 post-merge push run, 0 reruns; deploys: 0.
- **run evidence** — `.claude/runs/20260901-100229-codex-aic12-merge-loop`; PR #28; merge `ac569a04cc1903c49d847e11b0c3b98ab4b9d597`; post-merge run `33476000617`; Jira close comment `15425`; AIC-12 status/resolution read back as Done.
- **cleanup** — removed the merged retry-5 worktree plus local and remote `feat/aic-12-hitl-conclusion-review-retry-5` branches; earlier retry worktrees remain preserved as escalation evidence.
- **preflight** — `CAUTION`: kill switch absent; `RIG_RUN_DIR` initially unexported; `main == origin/main`; deploy history unavailable. A fresh run directory and budget were declared before merge/selection.

### AIC-12 retry 5 reached SHIP and stopped on the offline required runner

- **done** — AIC-12 retry 5 implemented exact own data-property DTO snapshots for the optional HITL conclusion review, closed the hidden/symbol field bypass and accessor TOCTOU regression-first, and opened PR #28 with `human-review` on exact pushed head `15ec53f3dfb1a47880919f3287b9c92b4d5cc0f2`; nothing was merged.
- **escalated** — required GitHub Actions job `checks` registered for the exact head but remained queued. GitHub reported `linux-arm64-01` offline; the Lima VM was already Running, while the enabled runner service was `inactive (dead)` and its status said verbatim `The runner registration has been deleted from the server, please re-configure.` The documented restart cannot replace a deleted registration. Jira comment `15416` asks the single unblock question: authorize the separate runner re-registration operation or bring `linux-arm64-01` online so the named check can run.
- **reviewed** — gate round 1 code/security review returned HOLD on changing-accessor TOCTOU for `threadId` and `interruptId`; three focused Red tests reproduced repeated reads and checkpoint mutation, then descriptor-snapshot validation closed them. Final round 2 code/security review returned SHIP with complete exact-head coverage. Local `npm run check` passed dependency boundaries, lint, build, and 178/178 tests with no skipped or todo tests. GitGuardian passed; hosted `checks` did not start.
- **stopped at** — merge prerequisite blocked by the offline required runner; after four reviewer subagent runs exhausted the declared reviewer budget, `run-state.mjs` recorded `budgetExhausted = true` and the installed selector returned the run-level `budget` stop. No CI bypass, merge, runner re-registration, third gate round, Rig upgrade, or generated Rig/Agent-OS patch was attempted.
- **unblocked** — nothing was released; AIC-12 remains In Progress and AIC-13 remains blocked by it until PR #28 passes required CI and receives human merge.
- **queue hygiene** — the installed `plan-md` queue was read fresh and contained 0 items and 0 proposals; the owner-directed Jira retry came from explicit chat authorization and Jira comment `15412`, not a substituted selector result. Jira comment `15415` recorded claim and scope, and `15416` recorded the CI stop.
- **cost** — reviewer subagent runs: 4; CI runs: 1 registered and queued, 0 completed; deploys: 0. The reviewer budget is exhausted, so this journal closing artifact is preserved on `docs/aic-12-retry5-ci-stop-journal` without a PR rather than bypassing a fresh prose gate.
- **run evidence** — `.claude/runs/20260901-080922-codex-aic12-retry5`; task branch `feat/aic-12-hitl-conclusion-review-retry-5`; PR #28; exact pushed head `15ec53f3dfb1a47880919f3287b9c92b4d5cc0f2`; required queued workflow run `33470391808`, job `99738794024`.
- **preflight** — `CAUTION`: kill switch absent; `RIG_RUN_DIR` initially unexported as required; `main == origin/main`; deploy history unavailable. The run then exported the fresh directory, persisted its budget before work, inspected the installed queue/state fresh, and left every pre-existing worktree untouched.

### AIC-12 retry 4 escalated after the final safety gate

- **escalated** — AIC-12 reached a documented stall at gate round 2 of 2 on pushed head `587273c031e5e6a2bd27bfcdf78a80f17662d5cf`: an adversarial resume config with own `threadId` plus own non-enumerable `checkpoint_id` crossed the public DTO and consumed the pending review; hidden and symbol extra fields also bypassed the start shape. Regression-first work had already added pre-invoke identity validation, a narrow `{threadId}` surface, raw-selector rejection, and own `kind`/`threadId` checks. The current hypothesis is that exact-shape parsing must compare `Reflect.ownKeys` with the allowed string-key set. Jira comment `15412` asks the single unblock question: authorize a fresh branch and reset gate budget for that remediation.
- **reviewed** — round 1 code/security review returned HOLD on prototype-chain bypasses; those were reproduced Red, fixed, and verified. Round 2 code/security review returned HOLD on the non-enumerable/symbol bypasses with complete exact-head coverage. Final local `npm run check` passed lint, build, and 167/167 tests with no skipped or todo tests; no PR was opened and hosted CI was not run.
- **stopped at** — the task stopped at `gate-rounds-exhausted`; after adapter escalation, the installed `plan-md` selector returned the run-level `queue-empty` stop. No third remediation/review round was invented.
- **unblocked** — nothing was released; AIC-12 remains In Progress and AIC-13 remains blocked by it.
- **queue hygiene** — the installed `plan-md` queue still contains 0 items and 0 proposals. Its adapter recorded the escalation streak but returned `ok: false` because a flat-list queue cannot mark the owner-directed Jira item escalated; the Jira diagnosis was posted through Rovo and the item was left claimed.
- **cost** — reviewer subagent runs: 4; CI runs: 0; deploys: 0. The declared per-run reviewer budget is exhausted, so this journal closing artifact is preserved on `docs/aic-12-retry4-stop-journal` without a PR rather than bypassing a fresh prose gate.
- **run evidence** — `.claude/runs/20260901-071933-codex-resume`; task branch `feat/aic-12-hitl-conclusion-review-retry-4`; exact pushed head `587273c031e5e6a2bd27bfcdf78a80f17662d5cf`; prior retry branches and worktrees remain unchanged.
- **preflight** — `CAUTION`: kill switch absent; `RIG_RUN_DIR` initially unexported as required; `main == origin/main`; deploy history unavailable. The run then exported the fresh directory above, persisted its budget before work, and inspected the installed queue/state fresh.

### Declared Codex loop stopped on the empty installed queue

- **stopped at** — `queue-empty`; the installed `plan-md` adapter returned no ticket, no skipped items, and a successful stop, so the run took no AIC work and did not substitute Jira, wait for Rig 0.6.3, or patch generated Rig/Agent-OS files
- **unblocked** — this queue has no dependency links; no ticket was selected or closed
- **queue hygiene** — 0 items and 0 proposals checked; nothing stale was reported; `.claude/queue.state.json` recorded `lastCompletedTier: normal`
- **cost** — before review of this stop record: reviewer subagents: 0; CI runs: 0; deploys: 0; no budget declaration was persisted before selection, which is a procedural gap, but selection started no task and consumed none of those resources
- **run evidence** — main-checkout trace `.claude/runs/20260901-011056-codex`; item-selection decision sequence 1 records `stopped queue-empty`; no improvement proposal was filed because the run found no actionable AIC or governance defect
- **preflight** —

  ```text
  **preflight** — verdict: CAUTION

  - pass · killSwitch — absent
  - pass · runDirNotExported — not exported
  - pass · defaultBranchFresh — main == origin/main
  - unknown · lastDeploy — could not read deploy history: Command failed: gh run list --workflow deploy --limit 1 --json conclusion,url

  _Not checked by this script — still yours (3):_
  - the queue is reachable through its adapter (`node .claude/scripts/queue/index.mjs next`)
  - no stray worktree from a dead session that this run might mistake for its own
  - a budget is declared for this run, and it is written down somewhere the run can re-read

  _An item skipped twice is the signal to script it or drop it: a checklist
  nobody completes decays into one nobody reads._
  ```

  Manual completion: the configured `plan-md` adapter was reachable and returned `queue-empty`; existing worktrees were treated as owned by other sessions and left untouched; the budget declaration was not written before selection, and that gap is recorded above rather than retroactively reported as complete.
